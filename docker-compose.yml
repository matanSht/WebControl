services:
  webcontrol:
    build: .
    ports:
      - "8080:8080"
    environment:
      - WC_HEADLESS=true
      - WC_MAX_SESSIONS=10
      - WC_LOG_JSON=true
      # - WC_API_KEY=your-secret-key
      # - WC_PROXY_SERVER=http://proxy:8080
    restart: unless-stopped
